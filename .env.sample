# Kube Resource Suggest Configuration

# Comma-separated list of namespaces to ignore during scanning
# Default: kube-system, local-path-storage, ingress-nginx
IGNORED_NAMESPACES=kube-system,monitoring,test-ns

# Path to your Kubernetes configuration file
# Default: ~/.kube/config (or in-cluster config)
# KUBECONFIG=/path/to/your/kubeconfig
